{
  "name": "@veral/authority",
  "version": "0.0.0",
  "private": true,
  "description": "Veral certificate authority — tier rules, policy, issuance, audit trail, synthesis.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@veral/shared": "workspace:*",
    "@veral/score": "workspace:*",
    "@veral/sources": "workspace:*",
    "@veral/attest": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "@types/node": "^22.10.0"
  }
}
