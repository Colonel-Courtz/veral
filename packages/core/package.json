{
  "name": "@veral/core",
  "version": "0.0.0",
  "private": true,
  "description": "Veral orchestration — subject resolution, public entry points for HTTP layer.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@veral/shared": "workspace:*",
    "@veral/sources": "workspace:*",
    "@veral/score": "workspace:*",
    "@veral/authority": "workspace:*",
    "@veral/attest": "workspace:*",
    "viem": "^2.21.55"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "@types/node": "^22.10.0"
  }
}
