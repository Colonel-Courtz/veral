{
  "name": "veral",
  "version": "0.0.0",
  "private": true,
  "description": "Veral — Verification Authority Layer for Ethereum.",
  "license": "MIT",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r --if-present lint",
    "build": "pnpm -r --if-present build",
    "dev": "pnpm --filter @veral/web dev"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "@types/node": "^22.10.0"
  }
}
